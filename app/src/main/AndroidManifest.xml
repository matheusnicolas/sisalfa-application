<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sisalfa.android.com.appsisalfa">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity android:name=".sisalfa.android.com.app.application.MainActivity" />
        <activity android:name=".sisalfa.android.com.app.application.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".sisalfa.android.com.app.application.ContextoActivity" />
        <activity android:name=".sisalfa.android.com.app.application.UserActivity" />
        <activity android:name=".sisalfa.android.com.app.application.DesafioActivity" />
        <activity android:name=".sisalfa.android.com.app.application.AddDesafioActivity"></activity>
    </application>

</manifest>